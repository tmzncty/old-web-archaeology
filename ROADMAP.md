# Roadmap

这不是产品发布路线，而是一条**研究可复核性逐步增加**的路线。AI 可以按阶段推进，但不能跳过证据基线直接做漂亮网页。

## M0 — 方法与数据合同

目标：让两个不同的人/AI 对同一旧站点，能够用同一格式留下证据。

- [x] `docs/METHOD.md`：A/B/C/D 证据等级细则；
- [x] `docs/CITATION.md`：archive URL、original URL、capture datetime 的引用规范；
- [ ] `schemas/evidence.schema.json`；
- [ ] `datasets/sites.csv` 字段定义；
- [ ] 规定“复原页面”必须携带 reconstruction 标识；
- [ ] 规定个人隐私/删除内容的再发布边界。

### M0 验收

随机给 3 个 archive capture，两个独立 Agent 生成的 `evidence.yaml` 关键字段应基本一致。

---

## M1 — 三个完整考古案例

不要先贪规模。

选择 3 类不同对象：

1. 一个个人主页/主页托管案例；
2. 一个论坛/BBS/社区案例；
3. 一个博客/SNS/平台案例。

每个案例至少包含：

- [ ] 身份确认：域名、标题、主体、活跃时期；
- [ ] ≥2 个历史时点；
- [ ] 尽可能 ≥2 个独立证据源；
- [ ] 资源缺失清单；
- [ ] 浏览器/编码/插件假设；
- [ ] “已证实 / 高概率 / 不知道”三栏；
- [ ] 不少于一处对后人回忆与同时代证据的比较。

### M1 禁止

- 只截几张 Wayback 图就宣称“复原完成”；
- 用今天的站点介绍替代历史证据；
- 自动补全已经丢失的图片并当成原件。

---

## M2 — 平台形态谱系

目标：从单站案例进入结构比较。

第一条建议主线：

```text
个人主页
→ 独立论坛 / BBS
→ 托管博客
→ 平台社区 / 贴吧
→ SNS / 信息流
```

研究维度：

- 谁拥有 URL？
- 谁决定页面布局？
- 链接是站长手工维护、友情链接，还是平台关系图？
- 身份是匿名昵称、站长身份还是实名/半实名账号？
- 内容的生命周期由谁决定？
- 站点死亡时，用户能否迁走内容？

产物：

- [ ] `studies/platform-genealogy.md`；
- [ ] `datasets/platforms.csv`；
- [ ] 至少 20 个平台/站点的时点证据；
- [ ] 一张“内容所有权与 URL 所有权变化”的比较图。

---

## M3 — 浏览环境复原实验

目标：证明“现代浏览器回放”与“历史浏览体验”之间的差异。

实验至少比较：

- modern Chromium；
- 一个 IE5/IE6 级环境；
- 一个 Netscape 级环境（条件允许）；
- Flash/Ruffle 或插件缺失场景。

记录：

- CSS/表格布局；
- 字符编码；
- JS 错误；
- ActiveX/Java/Flash；
- 字体与图片；
- viewport。

### 验收

选择一个案例，明确列出至少 5 个“浏览器环境导致的解释差异”。

---

## M4 — 缺失地图

成熟之后再做规模化索引。

不是只统计“保存了多少”，还要统计：

- 哪些年份没有 capture；
- 哪些资源类型系统性丢失；
- 哪些平台因 robots / 登录 / JS / 动态 URL 特别难保存；
- 哪些中文站点只剩第三方引用。

产物：

- [ ] archive-gap 数据模型；
- [ ] 100 个样本站点；
- [ ] 按平台/年份/资源类型统计缺失；
- [ ] 写出抽样偏差说明。

---

## AI 可直接领取的第一批任务

### Task A — 方法规范

读取 README + `docs/PRIOR_ART.md`，起草 `docs/METHOD.md`。重点定义 evidence 等级和 reconstruction 禁止事项，不写爬虫。

### Task B — 候选案例调查

列 15 个 1995–2010 中文旧网站/平台候选；逐一查 Wayback、ArchiveTeam、Common Crawl 等已有证据；输出 `research/case-candidates.md`，**只调查，不抓取**。

### Task C — 浏览器差异试验设计

用 oldweb.today / 现成虚拟机方案设计一个最小实验，证明同一历史页面在现代浏览器与旧浏览器的差异；先提交实验方案，不先造模拟器。

### Task D — 数据 schema

设计 `evidence.schema.json` 与 3 个 fixture，重点支持多个 archive source、多个 datetime、证据等级和 inference 标记。

## Stop conditions

AI 遇到以下情况必须停止扩张并记录：

- 已有成熟抓取/回放软件可以直接复用；
- 原始页面不可获得，只剩回忆；
- 涉及个人敏感/已删除内容；
- 版权状态不允许复制原资源；
- 无法区分复原材料与历史原件。

停止不是失败；“这个地方已经无法可靠复原”本身就是考古结论。
